import { Component } from '@angular/core';
import { Card } from '../core/card/card';

@Component({
  selector: 'app-skills-banner',
  imports: [ Card],
  templateUrl: './skills-banner.html',
  styleUrl: './skills-banner.scss',
})
export class SkillsBanner {
}
